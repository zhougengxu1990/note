# 什么是微服务
微服务不是发明出来的，是业务需求和技术演进出来的。采用分而治之的方法，在功能上将单体应用分割为更小的原子单位，每个原子单位只实现单一的功能。
与传统N层架构相比，每个微服务有各自的展现层、业务层和数据库层，与业务功能对应或一致，微服务之间是相互隔离的，一个微服务变更不会影响其他微服务
同时，DevOps和云计算也是微服务的两个重要方面

## Martin Fowler给出的定义
微服务架构风格是一种将单个应用开发为一套微小服务的方法。每个这样的微小服务在各自的进程内运行，采用轻量级的机制（通常是http资源API）与外界通信。这些微小服务通常围绕业务能力进行构建，并且可以用完全自动化的部署工具独立部署。微服务架构最起码要实现集中式服务管理，服务本身可能是用不同的编程语言和不同的数据存储技术实现的。
## 微服务架构设计原则
### 单一责任
### 服务自治
> 独立自治
- 微服务是自包含且可独立部署的自治服务，负责业务功能及其执行。微服务会捆绑所有依赖，包括第三方依赖、执行环境依赖（比如Web服务器和容器）和抽象了物理资源的虚拟机依赖。
- 微服务和SOA的一个主要区别在于服务的自治程度。虽然大多数SOA实现提供了服务级别的抽象，但微服务进一步抽象了实现和运行环境。
- 传统应用部署中，首先会构建一个或多个war或jar包，部署到JEE应用服务上，而微服务部署是将每个微服务构建为一个包含所有依赖的胖jar，然后在一个独立的Java进程中运行。微服务也可以在自己的容器中运行，容器是跨平台且可独立管理的轻量级运行环境。容器技术（比如Docker）是部署微服务的理想选择。

## 特性
### 服务是一等公民
微服务抽象了实现细节，对外通过API暴露服务端点，提供服务能力。
由于微服务或多或少像SOA，因此SOA中的许多服务特性也适用于微服务
- 服务合约：与SOA类似，微服务也是通过明确定义的服务合约来描述。JSON和REST广泛用于服务之间通信，同时也有很多技术如JSON Schema、WADL、Swagger和RAML等定义服务合约
- 松耦合：各自独立且松耦合的。通常通过消息机制、HTTP等实现服务间交互
- 服务抽象：不仅抽象服务实现的细节，更抽象所有库和环境细节
- 服务复用：微服务是粗粒度、可利用的服务
- 无状态化：无会话状态和共享状态。易于横向扩展和弹性伸缩
- 服务可发现性：服务会自我广播以便于外界发现，服务终结后它们会从微服务体系中自动消失。
- 服务互操作性
- 服务可组合性：服务编排和服务编制


### 微服务是轻量级的
因为设计良好的微服务对应单一业务功能，只实现一个功能，所以要求服务运行所需的资源较少，这也是一个必然要求。
选择微服务的支持技术，必须确保这些技术本身也是轻量级的，以确保服务整体资源开销是可控的。
- 如选择web容器，与传统Weblogic或WebSphere相比，选择Jetty或Tomcat作为微服务的容器更好
- 如与hypervisor技术（如VMware或Hyper-V）相比，容器技术（如Docker）有助于将基础设施的资源开销控制到最小

### 微服务是混合架构
> 本质是因为微服务之间独立不相互影响

- 不同微服务可以采用同一技术的不同版本开发
- 不同微服务可以选择不同的开发语言开发
- 不同微服务可以选择不同的存储架构开发

### 自动化必需性
由于微服务化将原来的单体应用打散为微服务，可能会出现微服务激增的情况，若非实现自动化，微服务将难以管理。
微服务较小的资源开销也有利于其从开发到部署的生命周期自动化
自动化生命周期：
- 自动化持续集成：开发阶段使用版本控制工具（如Git）和**持续集成**工具（如Jenkins、TravisCI等）实现自动化。
- 自动化测试：测试阶段使用自动化测试工具（如Selenium和Cucumber）和其他**A/B测试策略**实现自动化测试
- 自动化基础设施配置：通过容器技术、发布管理工具以及配置管理工具实现
- 自动化部署：可以使用SpringCloud、Kubernetes(k8s)等工具实现

### 生态支持系统
- DevOps流程
- 集中化日志管理
- 服务注册
- API网关
- 大量监控
- 服务路由
- 流量控制机制

### 微服务是动态分布式的
成功的微服务实现在服务中封装了逻辑和数据，这也导致了以下两种不寻常的状况：
- 分布式的数据和逻辑
- 去中心化的服务治理

从单体应用向微服务发展，原单体应用的边界发展为各自微服务创建自己的新的逻辑和数据边界
通常微服务集中式服务治理运行方式与SOA架构的不同，微服务实现的一个共性是不依赖于企业级重量级应用，如企业服务总线（ESB）。严重依赖静态服务注册和静态服务配置会产生额外的开销。这就是大多数微服务采用自动化的机制，利用系统的运行时拓扑来动态构建服务注册信息的原因。

### 抗脆弱、快速失败和自我愈合
**快速失败（fast fail）**是用于构建容错、弹性系统的另一个概念。提倡系统应接纳失败，而不是构建不出错的系统。关于在于一旦出错，系统如何从故障中快速恢复。这种系统设计方式的重心从**平均故障间隔时间（MTBF）**转向为平均恢复时间（MTTR）。关键优势在于某个功能出错，它会结束自身不影响下游功能。
自我愈合通常用于服务的部署中，系统会自动从故障中学习并调整自身，这种自我愈合系统也能避免之后出现错误

### 服务可替换性
微服务是自包含且可独立部署的模块，这使得可以将一个微服务替换为另一个相似的微服务
微服务可以选择购买或自开发，一些功能可以向外部购买实现功能补充。

<br/>

# 相关架构风格和用例
## SOA
SOA是一种支持面向服务的架构风格。面向服务是一种基于服务、服务开发和服务产出来思考问题的方式
### 面向服务的集成
“意大利面条式集成”，通常叫做**面向服务集成（SOI）**。各个服务通过公共集成层进行通信，使用标准协议和消息格式。
### 遗留系统现代化
有些IT转型项目或遗留系统现代化项目中使用SOA，将服务构建和部署在ESB上
### 面向服务的应用
在应用层面使用SOA

## 十二要素应用
基于云计算服务的“云原生”开发，将应用搬到云上，并充分理解和利用云计算特性，如弹性计算、按用量收费和失效感知等。
Heroku提出的十二要素应用是一种方法论，用于描述当代云计算应用应该具备几大特性，这些特性同样适用于微服务
### 单一代码仓库
每个应用应当只有其对应的一个代码仓库，同一代码库可以有多个部署实例，如不同环境的实例，通常用Git等版本控制系统管理代码

### 依赖捆绑
应用应将其应用包与外部依赖捆绑在一起，利用Maven或Gradle等构建工具在项目对象模型（POM）或gradle文件中显式管理第三方依赖，然后利用集中式构建件仓库（如Nexus或Archie）将这些依赖关联起来

### 配置外部化
所以配置信息都应该外部化，同时也有利于自动化部署和发布流程。

### 支撑服务 可寻址
所有支撑服务都应该可以通过可寻址的URL地址访问，而无须复杂的通信

### 构建、发布和运行时的隔离
倡导在构建阶段、发布阶段和运行阶段之间实行强隔离。
- 构建期是指将资源和代码编译和生成为二进制代码
- 发布期是指将二进制代码和环境特定配置参数整合在一起
- 运行期是指在特定运行环境中运行应用程序
该构建管道是意向的，不应为生产环境特定构建，所有构建都应通过构建管道构建出来
### 无状态、不共享进程
无状态，有利于水平扩展，方便动态处理并发问题。

### 通过端绑定暴露服务
端口暴露和提供服务

### 水平扩展实现高并发
### 最小开销实现可外置性
启动和关闭时间应当最小化，并且支持优雅关闭，包体积尽量小，也可以考虑延迟加载对象和数据

### 开发环境和生产环境对等性
### 日志信息外部化
不应该尝试存储和传输日志文件。使用集中式日志框架解决，捕获日志流并发送给集中式日志服务，以便保存和进一步分析
### 打包后台管理进程

## 无服务计算
无服务器计算基于微服务
## Lambda架构
## DevOps、云计算和容器
## 响应式微服务
- 挤压性
- 响应式
- 基于消息
- 弹性的
## 微服务用例
微服务不是灵丹妙药，不可能解决当前软件领域存在的所有架构问题。何时采用微服务架构不存在硬性规定或刻板准则
微服务架构并不适用于所有场景。微服务项目的成功很大程序上取决于场景的选择。


# 应用微服务 
微服务很好，但设计不当会很糟糕
> 不同设计决策之间的权衡取舍
> 开发微服务应当考虑的设计模式
> 设计一流的可扩展微服务的通用指南

## 微服务设计指南
通常微服务是构建轻量级、模块化、可扩展和分布式系统的一个不错选择。然而，过度设计、不当的应用和错误理解容易会导致系统灾难
### 确认微服务边界
> 领域驱动设计DDD定义了限界上下文的概念。限界上下文就是一个更大的域中的子域或一个大型系统中的子系统，它负责执行特定的业务功能

限界上下文有助于确定微服务边界。每个限界上下文都可以映射到单独的微服务。在现实世界中，限界上下文之间通信耦合度通常最低，并且相互之间往往是隔离的

虽然现实世界中组织边界是建立限界上下文的最简单机制，但由于组织架构内部的一些固有问题，在某些情况下这种机制可能是错误的。

自上而下的领域分解是建立正确的限界上下文的另外一种途径

### 设计微服务通信 
微服务通信可以使用同步或异步方式

### 微服务编排
第一种方式是orchestration，中央大脑的方式，统一编排。
第二种方式是choreography，通过生产--消费方式决定下游流程。
> choreography是将多个微服务连接起来的最佳方式，根据服务自治原则，由微服务之外的一个组件控制整个流程并不是理想选择
> 但在处理复杂流程、服务间交互和工作流时会变得复杂

### 每个微服务包含几个端口？
端口个数不是设计关键决策点，一个微服务可能包含一个或多个端口，重要是为微服务设计合适的限界上下文。

### 每个虚拟机运行多少个微服务

### 规则服务，共享还是嵌入
手写规则：规则只在当前微服务中使用，简单数量不多。
嵌入规则：如果规则比较复杂，仅限于服务上下文中使用，嵌入规则比较合适
本地规则引擎：如果规则更复杂，且由业务用户来编写，应集中规则仓库并在服务中嵌入本地规则引擎

### BPM和工作流的作用
业务流程管理（BPM）和智能业务流程管理（iBPM）
第一种情况是业务流程重组、或协调一个长时间运行的端到端的业务流程。在这种情况下，BPM在更高层次上运行，通过整合一系列粗粒度微服务、现有遗留系统的连接器以及人工干预的方式，可以将跨职能部门且长时间运行的业务流程自动化

第二种是监控流程执行和提高执行效率

### 微服务可以共享数据库吗
不。微服务是对外提供服务的抽象，最优解是不共享数据库。

### 微服务可以无头吗
### 确定事务边界
应避免分布式全局事务

为了确保事务边界尽可能不跨两个微服务，应详尽分析服务之间的依赖关系

### 服务端口设计考量点
合约设计
- 简洁性。力求适应未来提供复用性，实际这些适应未来变化的服务仍然会经历各种变化
- 易用性。复杂会降低可用性。KISS原则有助于开发出高质量服务，并降低服务维护和替换成本。YGANI(你不会需要它的)原则也提倡这种理念
- 演进式设计是很好的理念。提倡满足当前的需求，不断更新和重构，但不容易实现，需要有强大的服务治理能力


协议选择
> HTTP/SOAP和消息机制几乎是服务间交互的默认协议。松耦合是微服务的核心原则之一。
- 面向消息的服务
- HTTP和REST端口
- 优化的通信协议：对服务响应时间有严格要求，可以使用如protocal buffer、Thrift协议，提供性能。但这样会限制服务间的互操作性，需要在性能和互操作性之前有取舍
- API文档

### 处理共享类库
根本原则是自治和自包含，有时不得不复制部分代码和类库，涉及技术类库或功能组件

但这样做有悖于DRY原则（Don‘t Repeat Yourself），不利于维护、变更、增强或问题修复

### 微服务中UI
### 微服务中使用API网关
### 微服务中使用ESB和iPaaS
### 服务版本化考虑
MAJOR、MINOR、PATCH,MAJOR用于彻底不兼容的版本变更，MINOR用于向后兼容的版本变更，PATCH用于向后兼容的bug修复
### 跨域设计
### 处理共享的引用数据
### 微服务和批量操作

# 微服务能力模型
除了微服务本身，成功的微服务项目需要一系列相关生态系统的能力，保证这些前提条件到位非常重要
## 核心能力
### 服务监控器和类库
### 存储能力
选择合适的数据存储
### 服务实现
服务实现有多种方式，任意语言实现
### 服务端口

## 基础设施能力
### 云计算
### 容器运行时
### 容器编排
## 支撑能力 阿里云生态
### 服务网关 gateway
### 软件定义的负载均衡 lb
### 集中式日志管理 sls
### 服务发现 nacos
### 安全服务
单个微服务无法容纳所有安全主数据，安全成了突出问题。包括服务的认证和令牌服务
### 服务配置 
配置外部化，nacos config,阿里云生态
### 运维监控 arms
### 依赖管理
### 数据湖
数据分散到各自异构的数据孤岛中
需要一个数据仓库或数据湖汇总数据，进行数据分析和报表等
如何将数据汇总到数据湖
- 传统ETL工具（E-extract,提取；T-transform,转换；L-load，加载），依赖结构化转换数据，一般只适用于关系型数据库，处理转换逻辑复杂，有可视化的图形界面。关键在于破坏微服务的抽象原则
- 数据采集工具通过接收事件机制，并以恰当方式向数据湖传播服务状态变化。如SpringCloud数据流、Kafka、Flink和Flume等
### 可靠的消息机制
## 流程和治理能力
### DevOps
DevOps是成功实现微服务的关键之一

支持敏捷开发、快捷交付、自动化和变更管理，与微服务开发形成互补

### 自动化工具
测试自动化在微服务交付中极其重要

破坏性测试也是微服务开发中的一个实用技巧

### 容器注册表
### 微服务文档化

# 微服务演进案例研究
## PSS应用
### 业务流程视图
- 业务流程
- 可以区分面向客户还是面向内部
- 对于流程非连续作出标识
### 功能视图
每个业务流程与相关子功能列举展示
### 架构视图
### 设计视图
### 实现视图
### 部署视图

## 单体之死
### 痛点
业务快速增长给单体应用带来了压力
- 原本正常的代码可能不工作了
- 变更的成本变大以及交付速度变更
### 应急修复
对于出现的问题应急或临时修复，导致系统复杂度以及拥有成本急剧上升

## 迁移
### 方法
制定一份迁移计划，逐步将业务功能迁移到新的微服务
### 确定微服务边界
### 分析服务依赖关系
- 人工分析代码
- 开发团队经验
- maven依赖
- 性能监控工具确定调用栈并向上追溯依赖关系
### 微服务迁移优先级
- 依赖低的先迁移
- 复杂度低的先迁移
- 业务量大的先迁移，价值高
- 有利于提高资源利用率的
- 业务比较关键的，价值高
- 变更频率高的先迁移，利于更快交付
- 创新性
### 迁移过程中数据同步
- 数据同步工具同步
- 其他同步方式
- 旧数据如何处理
- 旧系统与新系统并行运行

# SpringCloudAlibaba扩展微服务
## NacosConfig
## Nacos服务注册、发现
## Gateway服务网关
## 保护微服务安全

# 日志和监控
# Docker容器化微服务
# 微服务生命周期
## 实践要点
### 理解业务动机和价值
基于微服务的开发是额外的开销

### 从项目开发到产品开发的观念转变
微服务更适合产品开发
通常产品团队与业务团队联系紧密，产品为业务提供质量好的产品，同时IT也需要与产品联系紧密

### 选择正确的开发理念
### 使用最小可行产品概念
最小可行产品，找到真实需求，MVP

### 克服遗留热点
### 建立自组织团队
小而专注于特定方向的团队能交付最佳结果

### 构建自服务云
### 构建一套微服务生态
### 以DevOps实践贯穿微服务开发生命周期
- 持续集成：定时或在开发人员提交时，持续自动化对应用进行构建和质量检查。持续集成会将代码检查相关指标发布到一个集中式的仪表盘中，会将二进制构件发布到一个集中式的构件仓库中
- 持续交付：产品从构想到生产过程中，将端到端的软件交付实践活动自动化。将QA测试周期加入到集成管道，软件准备就绪可随时发布到生产环境
- 持续部署：将二进制代码自动部署到一或多个环境中
- 应用发布自动化：ARA工具。

### 价值驱动计划
开发哪些微服务，最重要的是确定对业务而言价值最高风险最低的需求进行开发
- 从头开发微服务需要MVP原则
- 迁移时考虑优先级原则

### 持续监控和反馈
## 自动化开发周期
### 开发
- 源代码 
- 单元测试用例
- 消费者驱动合约
- 模拟测试
- 行为驱动设计
- 源代码库
- 构建工具
- 构建仓库
- 数据库schema

### 集成
- 构建和QA测试。可以使用Git的webhook勾子，监控代码提交，构建、触发自动执行一系列QA检查
- 打包。持续集成会创建一个可部署的软件包
- 集成测试。部署到集成测试环境中，进行回归测试（服务测试）

### 测试
- 可用性测试，基于可用性测试用例保证所有基本功能可以正常工作，确保服务启动，依赖可用
- 回归测试。回归测试保证软件中的变更不会破坏整个系统。
- 功能测试。使用功能测试用例测试保证功能正常
- 验收测试。自动化验收测试通常使用录屏回放或用户旅程模式，耐用应用于web层面，通常从用户视角，而不是功能视角，这些测试会模拟用户的操作流程
- 性能测试。一些工具可以集成到Jekins工作流中，从而实现自动化性能压力测试
- 真实用户操作流程模拟或用户旅程测试
- 安全性测试
- 探索性测试，人工测试，不期望结果的测试
- A/B测试、金丝雀测试和蓝绿部署。A/B测试用于检测某个变更有效性及市场反应。金丝雀将新功能推送给特定的群体。蓝绿发布部署用于将一个环境无缝切换到另一个环境
- 其他非功能性测试
- 生产环境测试
- 搞脆弱性测试。一般在预发布环境，甚至在生产环境中测试。观察应用从故障中自动恢复能力。

### 部署
### 监控和反馈
### 配置管理 
从微服务和DevOps角度考虑配置管理，不应考虑人工维护
### 微服务开发治理、参考架构和类库