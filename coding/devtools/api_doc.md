## 返回值类型的选择
### true/false or 1/0
对于一个是或否的状态位，历史使用1/0的方式比较常见，但如果只是表示是or否，那么建议使用返回true/false。更符合语义，也更容易理解

### 数组类型如果是空值，返回null or array[]
对于一个返回字段是数据类型，如果是空值，返回哪个合适
- 返回null，对于一些语言，如java比较容易编码的返回方式是null
- 返回空数组[]，这个有利于前端处理，不需要再对返回值进行兼容性转化处理（如 list = list || []）
结论：推荐直接返回空数组数据，而不是Null.